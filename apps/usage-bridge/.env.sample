# Usage Bridge Service Configuration Example
# Copy this file to .env and modify as needed

# Service Configuration
BRIDGE_HOST=0.0.0.0
BRIDGE_PORT=8001
BRIDGE_DEBUG=true
BRIDGE_LOG_LEVEL=INFO

# Claude Monitor Integration
# BRIDGE_CLAUDE_MONITOR_PATH=/path/to/claude-monitor
# Leave commented for auto-detection

# CORS Settings (JSON array format)
BRIDGE_CORS_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173", "http://localhost:4000", "http://127.0.0.1:4000"]

# Rate Limiting
BRIDGE_RATE_LIMIT_PER_MINUTE=60

# Configuration Storage
# BRIDGE_CONFIG_STORAGE_PATH=~/.claude-monitor-bridge
# Leave commented for default